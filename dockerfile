FROM node:alpine

WORKDIR /app
COPY . .
RUN /bin/sh -c 'npm i'

ENV PORT=3000
ENV DB_USERNAME=
ENV DB_PASSWORD=
ENV MARIADB_ROOT_PASSWORD=
ENV DB_NAME=test
ENV DB_PORT=
ENV JWT_SECRET=


EXPOSE 3000

CMD [ "npm", "start" ]